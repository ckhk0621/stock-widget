# Stock API Configuration

# API Provider: Choose between 'finnhub' (recommended) or 'alphavantage'
# Default: finnhub
VITE_STOCK_API_PROVIDER=finnhub

# Finnhub API Key (FREE: 60 calls/min, ~86,400/day)
# Get your free API key at: https://finnhub.io/register
VITE_FINNHUB_API_KEY=your_finnhub_api_key_here

# Alpha Vantage API Key (FREE: 25 calls/day, 5 calls/min)
# Get your free API key at: https://www.alphavantage.co/support/#api-key
# Note: Only needed if you set VITE_STOCK_API_PROVIDER=alphavantage
VITE_ALPHA_VANTAGE_KEY=demo

# Alpha Vantage: Enable OVERVIEW API call for comprehensive data
# OVERVIEW provides: Market Cap, PE Ratio, PB Ratio, EPS, 52-week high/low, Exchange
# Setting to 'false' reduces API calls from 3 to 2 per page load (50% more daily capacity)
# Default: false (disabled to conserve API quota)
# Set to 'true' only if you need comprehensive fundamental data
VITE_ENABLE_OVERVIEW=false

# Upstash Redis Configuration (Optional - Server-side Caching)
# Enables shared cache across all clients for massive API savings
# Without Upstash: 100 visitors = 300 API calls (each browser caches separately)
# With Upstash: 100 visitors = 3 API calls (all share same Redis cache)
# Get your free Redis at: https://upstash.com/
UPSTASH_REDIS_REST_URL=your_upstash_redis_rest_url_here
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_rest_token_here
