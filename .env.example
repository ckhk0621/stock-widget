# Stock API Configuration

# API Provider: Choose between 'upstash' (recommended) or 'alphavantage'
# Default: alphavantage
VITE_STOCK_API_PROVIDER=alphavantage

# Alpha Vantage API Key (FREE: 25 calls/day, 5 calls/min)
# Get your free API key at: https://www.alphavantage.co/support/#api-key
VITE_ALPHA_VANTAGE_KEY=demo

# Alpha Vantage: Enable OVERVIEW API call for comprehensive data
# OVERVIEW provides: Market Cap, PE Ratio, PB Ratio, EPS, 52-week high/low, Exchange
# Setting to 'false' reduces API calls from 3 to 2 per page load (50% more daily capacity)
# Default: false (disabled to conserve API quota)
# Set to 'true' only if you need comprehensive fundamental data
VITE_ENABLE_OVERVIEW=false

# Vercel Deployment URL (Required for Upstash provider)
# This URL hosts the serverless API routes (/api/stock/*)
# Required when widget is embedded in WordPress or other external sites
# Leave empty for local development (uses window.location.origin)
# Production example: https://your-app.vercel.app
VITE_VERCEL_URL=

# Upstash Redis Configuration (Recommended - Server-side Caching)
# Set VITE_STOCK_API_PROVIDER=upstash to enable Redis caching
#
# Benefits:
# - Shared cache across all clients (100 visitors = 3 API calls vs 300)
# - Massive API savings (99% reduction in API calls)
# - Instant load for all users after first request
#
# Setup:
# 1. Get free Redis at: https://upstash.com/
# 2. Copy REST URL and Token from Upstash dashboard
# 3. Set VITE_STOCK_API_PROVIDER=upstash
# 4. Set VITE_VERCEL_URL to your Vercel deployment URL
# 5. Test locally: npm run build && npm run preview
#
# Note: Server-side env vars (below) must also be set in Vercel dashboard
UPSTASH_REDIS_REST_URL=your_upstash_redis_rest_url_here
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_rest_token_here
